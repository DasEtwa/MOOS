import SwiftUI

struct SettingsPlaceholderView: View {
    var body: some View {
        ShellPlaceholderView(
            title: "Settings",
            symbolName: "gearshape",
            message: "Local shell preferences will live here."
        )
    }
}
